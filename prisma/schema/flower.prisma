model Flower {
  id        Int      @id @default(autoincrement())
  name      String
  color     String
  price     Float
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("flowers")
}
